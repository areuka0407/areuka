$(function(){
    findPrevLogin();
});


// 이전 로그인 정보 찾기
function findPrevLogin(){
    var ls_data = localStorage.getItem("prevLogin");
    if(ls_data !== null){
        // $("")

        /*
        로그인 기능이 구현 된 후, 어떤 식으로 토큰을 발급해서
        회원들을 처리할 것인지부터 고려해야 할 것.
         */
    }
}